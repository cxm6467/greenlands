{{flutter_js}}
{{flutter_build_config}}

_flutter.loader.load({
  config: {
    // Enable color emoji support to fix font warnings
    useColorEmoji: true,
    // Optional: Configure font fallback
    fontFallbacks: ['Roboto', 'sans-serif'],
  },
});
